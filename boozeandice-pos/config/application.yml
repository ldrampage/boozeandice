server:
  port: 8080
  address: lyndon
  
spring:
  profiles:
    active: dev
  application:
    name: boozeandice
  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 2MB
      enabled: true


#application settings
business_name: Booze and Ice
senior_citizen_discount: 0.20 #20 PERCENT SENIOR CITIZEN DISCOUNT
packaging_fee: 7.5 
productSilogCategory: 'silog meals'

#img upload location
upload:
  directory: C:\Projects\boozeandicepos\boozeandice-pos\boozeandice\boozeandice-pos\img


  
logging:
  level:
    root: INFO
    org.hibernate: INFO
    com.boozeandice: DEBUG
  pattern:
    console: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%p %thread %F:%L] %msg%n'
    file: '%d{yyyy-MM-dd HH:mm:ss.SSS} [%p %thread %F:%L] %msg%n'
  file:
    path: 'C:\workspace\loggers\boozeandice'
    name: '${logging.file.path}/${spring.profiles.active}.${spring.application.name}-${date:yyyy-MM-dd}.log'

---
 
spring: 
  config:
    activate:
      on-profile: dev
  datasource: 
    url: jdbc:mysql://localhost:3306/boozeandice
    username: root
    password: lxbordo
    #driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        generate-ddl: true
        dialect: org.hibernate.dialect.MySQLDialect
        ddl-auto: create
    hibernate:
      ddl-auto: create
  cache:
    type: simple
     